; Maps a content pool to items found in the pool.
; Format is lootbox_[content pool name]
; Each entry is an item that can potentially be rolled, and the max amount.
; ex) duct_tape = 3 means that in that pool, if duct tape is picked, up to 3 duct tapes spawn.

#include "contents_*.ltx"