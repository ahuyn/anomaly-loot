; Loot pools define the contents of a 
; Loot pools are leveraged by 'grab' type lootboxes. 
; Format is 'lootbox_[name of pool]' e.g. if your new box picks from 'custom' pool, specify section 'lootbox_custom'
; Format of each entry is 1 or more of [item pool][num] where item pool maps to a contents pool
; You can append |1, |2, |3 etc to duplicate entries

#include "pools_*.ltx"